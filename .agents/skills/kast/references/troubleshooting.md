# Troubleshooting note

This document is intentionally minimal.

If the packaged skill cannot find the binary, inspect
`.agents/skills/kast/scripts/resolve-kast.sh`. For command usage, request
schemas, and evaluation flows, use `.agents/skills/kast/SKILL.md`.

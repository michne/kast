# Cloud setup note

This document is intentionally minimal.

The packaged kast skill no longer ships or documents shell wrapper scripts. Use
`.agents/skills/kast/SKILL.md` as the sole source of truth for bootstrap,
`kast skill ...` command usage, and `kast eval skill`.

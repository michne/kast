# LLM scaffolding reference

Kast's packaged skill no longer uses the deleted helper scripts that previously
wrapped the CLI. The supported semantic entrypoints are the native
`kast skill ...` subcommands documented in `.agents/skills/kast/SKILL.md`.

Use that file plus `references/wrapper-openapi.yaml` for current request shapes
and examples.
